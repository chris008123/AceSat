# Vector retrieval (Phase 7 — optional, not yet built)

Only build this if the MVP demo actually needs semantic search over
concept explanations (per hackathon prompt section 10). Requires
pgvector enabled on the Postgres instance — coordinate with the Backend
Engineer before adding the extension.
